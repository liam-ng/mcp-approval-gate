# Runtime dependencies — single source of truth.
# pyproject.toml reads this file (dynamic dependencies) and the Dockerfile
# installs from it, so the lists cannot drift.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
authlib>=1.3
itsdangerous>=2.2
httpx>=0.27
boto3>=1.34
python-ulid>=2.6
# MCP Streamable HTTP server + OAuth2.1 Resource Server support for /mcp
# (backend/app/api/mcp_gateway.py). Pinned to the 2.x line this was built
# and tested against — the SDK's public API has moved fast (FastMCP was
# renamed MCPServer between 1.x and 2.0).
mcp>=2.0.0,<3
