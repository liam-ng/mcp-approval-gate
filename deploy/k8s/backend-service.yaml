apiVersion: v1
kind: Service
metadata:
  name: mcp-approval-gate-backend
  namespace: mcp-approval-gate
spec:
  # Selector stays the bare `app` label -- see backend-deployment.yaml.
  selector:
    app: mcp-approval-gate
  ports:
    - port: 80
      targetPort: 8000
