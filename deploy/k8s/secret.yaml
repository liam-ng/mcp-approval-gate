# TEMPLATE ONLY — do not commit real values. Prefer External Secrets Operator
# or Sealed Secrets; this file documents the required keys.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-approval-gate
  namespace: mcp-approval-gate
type: Opaque
stringData:
  SESSION_SECRET: "CHANGE_ME_LONG_RANDOM"
  OIDC_CLIENT_SECRET: "CHANGE_ME"
