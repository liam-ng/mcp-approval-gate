apiVersion: v1
kind: Service
metadata:
  name: mcp-approval-gate-frontend
  namespace: mcp-approval-gate
spec:
  selector:
    app: mcp-approval-gate-frontend
  ports:
    - port: 80
      targetPort: 8080
